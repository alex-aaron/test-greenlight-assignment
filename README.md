# test-greenlight-assignment

### Test Assignment

1. Create a function called `reduceArray` that takes in an `array` of numbers. This function should use the native reduce method to return a new array of only the even numbers.